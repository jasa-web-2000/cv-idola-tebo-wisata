<div class="flex items-center py-2">
    <a class="block overflow-hidden hover:rotate-3" href="{{ route('home') }}" title="{{ web()->title }}">
        <img class="w-24 h-auto" title="{{ web()->title }}" src="{{ web()->transparentLogo }}"
            alt="Logo {{ web()->title }}" width="80" height="40" />
    </a>
</div>
